{
  "name": "02-weather",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  },
  "scripts": {
    "dev": "bun run index.ts --watch",
    "run": "bun run index.ts",
    "build": "bun build index.ts --compile --outfile weather"
  }
}
