{
  "name": "02-weather",
  "version": "1.1.0",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  },
  "scripts": {
    "dev": "bun run index.ts --watch",
    "run": "bun run index.ts",
    "test": "bun test",
    "build": "bun build index.ts --compile --outfile weather"
  }
}
